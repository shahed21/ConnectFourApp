This is a C Application for Connect Four.
It is done using 64 unsigned integers to represent the tokens on the entire board.

in gnu Windows 32bit Application  compile using the following command.

> gcc *.c -o main.exe

then execute the application by calling the main.exe file from the console.

> .\main.exe

Human play 'x'.  Human plays first.